import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  // 데이터가 들어가는 object
  state: {
  },
  // 명령 수행 (데이터를 바꾸는 역할 mutations에서만 가능)
  mutations: {
  },
  // 함수가 들어가서 비동기처리, action에서 함수가 완료되면 mutation에서 데이터를 업데이트 
  actions: {
  },
  // computed
  modules: {
  }
})
